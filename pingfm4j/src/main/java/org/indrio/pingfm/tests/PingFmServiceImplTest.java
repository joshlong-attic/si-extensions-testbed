package org.indrio.pingfm.tests;

public class PingFmServiceImplTest {
    /*

    private static final Message TEST_MESSAGE = new Message(null, "default");

    private PingFmService pingFmService;

    @Before
    public void setUp() {
        pingFmService = new PingFmServiceImpl(
                SettingProperties.getDeveloperKey(),
                SettingProperties.getApplicationKey());

        assertNotNull(pingFmService);
    }

    @Test
    public void testValidate() {
        assertTrue(pingFmService.validate());
    }

    @Test
    public void testGetAllSupportedService() {
        List<Service> services = pingFmService.getAllService();

        assertNotNull(services);

    }

    @Test
    public void testGetUserLatestMessage() {
        List<Message> messages = pingFmService.getUserLatestMessage("5");

        assertNotNull(messages);
        assertEquals(5, messages.size());
    }

    @Test
    public void testGetUserSupportedService() {
        List<Service> services = pingFmService.getUserService();

        assertNotNull(services);
    }

    @Test
    public void testGetUserTriggers() {
        List<Trigger> triggers = pingFmService.getUserTriggers();

        assertNull(triggers);
    }

    @Test
    public void testPostMessageDebug() {
        TEST_MESSAGE.setContentTitle("test title");
        TEST_MESSAGE.setContentBody("test body");

        assertTrue(pingFmService.postMessage(TEST_MESSAGE, "default", true));
    }

    */
/*
     @Test
     public void testPostMessage() {
         TEST_MESSAGE.setContentTitle("testing using pingfm4j");
         TEST_MESSAGE.setContentBody("testing posting using http://code.google.com/p/pingfm4j/");

         assertTrue(pingFmService.postMessage(TEST_MESSAGE, "default", false));
     }
     *//*


*/
}
