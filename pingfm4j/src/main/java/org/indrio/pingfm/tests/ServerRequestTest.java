package org.indrio.pingfm.tests;

public class ServerRequestTest {
    //   private ServerRequest serverRequest;
/*
    @Before
    public void setUp() {
        serverRequest = new ServerRequestImpl();
    }*/

    /*  @Test
    public void testSend() {
        Map<String, String> params = new HashMap<String, String>();
        params.put(ServerRequest.PARAM_API_KEY, SettingProperties.getDeveloperKey());
        params.put(ServerRequest.PARAM_USER_APP_KEY, SettingProperties.getApplicationKey());

        try {
            assertNotNull(serverRequest.send(SettingProperties.getLocationHost(), "/"
                    + (SettingProperties.getApiVersion() + "/" + PingFmService.ACTION_USER_VALIDATE), params));
        } catch (IOException e) {
            e.printStackTrace();
        }
    }*/
}
