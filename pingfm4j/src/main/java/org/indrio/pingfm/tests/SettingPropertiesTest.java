package org.indrio.pingfm.tests;

public class SettingPropertiesTest {
    public void testFoo() throws Throwable {
    }
/*
    private static final String APP_NAME = "Ping.fm Java API";
    private static final String APP_VERSION = "0.1";

    private static final String API_LOCATION_HOST = "api.ping.fm";
    private static final String API_VERSION = "v1";
    private static final String API_LOCATION_URL = "http://" + API_LOCATION_HOST + "/" + API_VERSION;

    private static final String API_DEVELOPER_KEY = "";
    private static final String API_APPLICATION_KEY = "badb1b1594ce9cdb52f92c8fe7f2ffc9-1220130174";

    @Test
    public void testGetAppName() {
        String appName = SettingProperties.getAppName();
        assertNotNull(appName);
        assertEquals(APP_NAME, appName);
    }

    @Test
    public void testGetAppVersion() {
        String appVersion = SettingProperties.getAppVersion();
        assertNotNull(appVersion);
        assertEquals(APP_VERSION, appVersion);
    }

    @Test
    public void testGetLocationURL() {
        String locationHost = SettingProperties.getLocationHost();
        assertNotNull(locationHost);
        assertEquals(API_LOCATION_HOST, locationHost);

        String apiVersion = SettingProperties.getApiVersion();
        assertNotNull(apiVersion);
        assertEquals(API_VERSION, apiVersion);

        String locationUrl = "http://" + locationHost + "/" + apiVersion;
        assertEquals(API_LOCATION_URL, locationUrl);
    }

    @Test
    public void testGetDeveloperKey() {
        String developerKey = SettingProperties.getDeveloperKey();
        assertNotNull(developerKey);
        assertEquals(API_DEVELOPER_KEY, developerKey);
    }

    @Test
    public void testGetApplicationKey() {
        String applicationKey = SettingProperties.getApplicationKey();
        assertNotNull(applicationKey);
        assertEquals(API_APPLICATION_KEY, applicationKey);
    }*/

}
